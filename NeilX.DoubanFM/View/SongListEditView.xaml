<Page
    x:Class="NeilX.DoubanFM.View.SongListEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeilX.DoubanFM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator},Path=SongListEditVM}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="编辑歌单信息"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="{x:Bind SongListEditVM.SelectList.Name}"/>
            <TextBlock Text="{x:Bind SongListEditVM.SelectList.BuildTime}"/>
            <Button Content="确定" Click="{x:Bind SongListEditVM.UpdateInfo}"/>
            <Button Content="取消"  Click="{x:Bind SongListEditVM.Cancel}"/>
        </StackPanel>
    </Grid>
</Page>
