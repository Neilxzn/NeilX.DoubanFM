<Page
    x:Class="NeilX.DoubanFM.View.MyMusicView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeilX.DoubanFM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:NeilX.DoubanFM.Core"
    mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator},Path=MyMusicVM}">
    <UserControl.Resources>
        <DataTemplate x:Key="songItemDataTemplate" x:DataType="core:Song">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind Title}"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot >
            <PivotItem Header="本地音乐">
                <Grid>
                    <ListBox ItemsSource="{x:Bind MyMusicVM.AlllocalSongs}"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="电台收藏">
                <Grid/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
